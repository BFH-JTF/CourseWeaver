# DocPouch backend settings (defaults for local dev)
# These are only used as fallback — the user configures them in the Settings UI
# and the values are persisted in localStorage.
VITE_APP_TITLE=CourseWeaver